This repository contains data about semantic shifts, obtained from [DatSemShift](https://datsemshift.ru) via `pycldf`. To recreate the TSV files, simply run the Python script:

```
python getshifts.py
```
