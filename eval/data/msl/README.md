This directory contains similarity ratings obtained from [Multi-SimLex](https://multisimlex.com) via `pyconcepticon`. To recreate the CSV file, simply run the Python script:

```
python create_multisimlex_data.py
```
