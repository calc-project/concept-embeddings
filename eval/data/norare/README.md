The evaluation script `eat.py` assumes that NoRaRe data is stored within this directory. For downloading the data accordingly, simply run:

```bash
make
```

To clean up the data afterwards, you can run:

```bash
make clean
```
